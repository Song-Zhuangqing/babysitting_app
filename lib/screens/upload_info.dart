import 'package:flutter/material.dart';

class UploadInfoScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Upload Information'),
      ),
      body: Center(
        child: Text('Information Upload Form'),
      ),
    );
  }
}
