import 'package:flutter/material.dart';

class MatchNeedsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Match Needs'),
      ),
      body: Center(
        child: Text('Matching Algorithm Here'),
      ),
    );
  }
}
